API_KEY=__replace__

AWS_REGION=ap-southeast-2
S3_BUCKET=__replace__
S3_PRESIGN_EXPIRE=900

DB_URL=postgresql+psycopg2://<user>:<pass>@<host>:5432/<db>

CORS_ORIGINS=http://localhost:3000,https://your-vercel-app.vercel.app

# === Phase 2-4 model bundle locations (two-file dirs) ===
BASELINE_BUNDLE_PATH=/opt/cvitx/models/baseline
CMT_BUNDLE_PATH=/opt/cvitx/models/cmt

# Optional: backbones (override per variant if needed)
BACKBONE_NAME_BASELINE=mobilevitv2_200
BACKBONE_NAME_CMT=mobilevitv2_200
